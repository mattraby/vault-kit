# Outputs

Long-form documents synthesized **from** the wiki and written for a reader outside this vault.
Unlike `wiki/` pages (atomic concepts, maintained in place), these are whole documents with an
intended audience. See the Publish workflow in [AGENTS.md](../AGENTS.md#workflows).

Every output belongs in its topic's catalog. Outputs are entry points for outside readers rather
than nodes in the graph, so nothing else links to them — the index is how they get found.

## Topics

- [Topic One](topic-one/index.md) — what this research area covers, in a line.
